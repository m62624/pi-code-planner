# verification

Verify the active work item. Run configured checks when available, inspect failures, and decide whether to return to refactor, experiments, or commit.

# details

Verification must be evidence-based. Do not claim success without command output, test results, or concrete static inspection.
